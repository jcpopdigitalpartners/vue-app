# Browser-safe settings only (no API keys here)
# heuristic = local scoring, api = call backend assessment service (Gemini)

VITE_ASSESSMENT_PROVIDER=heuristic
# VITE_ASSESSMENT_PROVIDER=api
# VITE_ASSESSMENT_API_URL=/api

# Production (GitHub Pages): point to your Railway API, e.g.
# VITE_ASSESSMENT_PROVIDER=api
# VITE_ASSESSMENT_API_URL=https://your-app.up.railway.app/api
